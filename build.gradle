group 'com.op'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.9

repositories {
    jcenter()
}

dependencies {
    compile fileTree(dir: 'lib')
    runtime fileTree(dir: 'lib')

    testCompile group: 'junit', name: 'junit', version: '4.12'

}
